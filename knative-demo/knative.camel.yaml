- route:
    id: route-ad87
    from:
      uri: knative
      id: from-97a0
      parameters:
        environment: '#bean:knativeConfiguration'
      steps:
        - log:
            message: ${body}
            id: log-64de
